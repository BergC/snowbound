const initialState = {
    address: {
        street: '',
        city: '',
        state: '',
        zip: '',
    },
    criteria: {
        filter: '',
        upperBound: '',
    },
};

export default function (state = initialState, action) {
    return state;
}
